'use client';
import { Sun, Moon, Check, Cookie, Palette, UserRound, SlidersHorizontal } from 'lucide-react';
import { Tabs, TabsList, TabsTrigger } from '@/components/ui/tabs';
import { Shell } from './shell';
import { usePreferences } from './preferences';
import { Field, Toggle } from './builder';
export function SettingsPage() { const { preferences, update } = usePreferences(); return <Shell page="settings"><div className="page-heading"><div><p className="eyebrow">A SPACE THAT WORKS FOR YOU</p><h1>A few personal touches.</h1><p>Set up your workspace and review your assessment details.</p></div></div><div className="settings-grid"><div className="settings-stack"><section className="content-panel"><div className="card-title"><span className="feature-icon"><Palette size={20}/></span><h2>Appearance</h2></div><p>Your theme also carries through to downloaded activities.</p><Field id="theme-choice" label="Colour theme"><Tabs value={preferences.theme} onValueChange={v => update({ theme: v as 'light' | 'dark' })}><TabsList id="theme-choice" className="theme-options" aria-label="Colour theme"><TabsTrigger className="theme-option" value="light"><Sun size={20}/>Light</TabsTrigger><TabsTrigger className="theme-option" value="dark"><Moon size={20}/>Dark</TabsTrigger></TabsList></Tabs></Field><Toggle id="compact-layout" label="Compact layout" help="Reduce spacing in the teacher workspace." checked={preferences.compact} onChange={compact => update({ compact })}/><p className="preferences-message"><Cookie size={15}/>Preferences are saved in a cookie on this browser.</p></section><section className="content-panel"><h2>Student-friendly by design</h2><p>Phoneme hints, English labels and difficulty are set separately in each activity builder. Choose the amount of support that suits your class.</p><p className="field-help" style={{ marginBottom: 0 }}>Keyboard navigation, visible focus indicators and feedback symbols are included in both themes. Activities have no timer.</p></section></div><section className="content-panel"><div className="card-title"><span className="feature-icon"><UserRound size={20}/></span><h2>Assessment details</h2></div><p>These appear on the About page and in the website footer. Samuel Karanja · 22301707 is included by default.</p><Field id="student-name" label="Your name"><input id="student-name" className="text-input" autoComplete="name" maxLength={80} placeholder="Enter your full name" value={preferences.name} onChange={e => update({ name: e.target.value })}/></Field><Field id="student-number" label="Student number"><input id="student-number" className="text-input" inputMode="numeric" maxLength={30} placeholder="Enter your student number" value={preferences.studentNumber} onChange={e => update({ studentNumber: e.target.value })}/></Field><div className="small-info"><InfoIcon /><span>Details are saved only in this browser. They are not included in classroom game downloads.</span></div><p className="preferences-message"><Check size={15}/>Changes save automatically on this device.</p></section></div></Shell>; }
function InfoIcon() { return <SlidersHorizontal size={15}/>; }
