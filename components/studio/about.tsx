'use client';
import { useEffect, useState } from 'react';
import Link from 'next/link';
import { Video, Upload, BookOpen, UserRound } from 'lucide-react';
import { toast } from 'sonner';
import { Shell, HowItWorks } from './shell';
import { usePreferences } from './preferences';
import { Field } from './builder';
export function AboutPage() {
    const { preferences, update } = usePreferences();
    const [localVideo, setLocalVideo] = useState('');
    const [urlDraft, setUrlDraft] = useState(preferences.videoUrl);
    const [videoError, setVideoError] = useState(false);
    useEffect(() => () => { if (localVideo)
        URL.revokeObjectURL(localVideo); }, [localVideo]);
    const videoSource = localVideo || preferences.videoUrl;
    function setVideoUrl() { if (!urlDraft.trim()) {
        update({ videoUrl: '' });
        setVideoError(false);
        return;
    } try {
        const url = new URL(urlDraft);
        if (url.protocol !== 'https:')
            throw new Error();
        update({ videoUrl: url.href });
        setVideoError(false);
        toast.success('Video link saved on this device.');
    }
    catch {
        toast.error('Enter a full HTTPS link to a playable MP4 or WebM video.');
    } }
    return <Shell page="about"><div className="page-heading"><div><p className="eyebrow">THE THINKING BEHIND THE SOUNDS</p><h1>A little play, with a purpose.</h1><p>A phoneme activity builder for teachers and speech pathology students.</p></div><HowItWorks /></div><p className="about-intro">Phoneme Studio makes it easy to prepare sound-based classroom activities. Teachers choose the support their students need, preview the experience, then download one HTML file to use in a normal browser.</p><div className="about-grid"><div className="settings-stack"><section className="content-panel"><div className="card-title"><span className="feature-icon"><BookOpen size={20}/></span><h2>Assessment 1 · Frontend only</h2></div><p>This version was created using npx create-next-app . and is built with Next.js, React and reusable components. Configuration, game previews and HTML generation run in the browser. There is no database, sign-in system or dynamic word-list management.</p><p>Wordle uses one selected answer from the supplied <strong>HCE Wordle Phoneme Corpus</strong>: 90 words, with 30 each of three, four and five phonemes. Word Search adapts the supplied HTML example and starts with chin, bait, jam, bad and boot, with all ten examples available as a second preset. Database-driven word lists remain reserved for Assessment 2.</p><p>The corpus and example file are stored with the source code. Their phoneme boundaries are preserved; the source’s g and ɡ spellings are treated as the same sound. These activities support phoneme recognition and sound sequencing. Example spellings are cues for sounds; teachers can model pronunciations appropriate to their students’ accent and learning needs.</p><div className="author-card"><span className="avatar"><UserRound size={20}/></span><div><strong>{preferences.name || 'Your name'}</strong><p>{preferences.studentNumber ? `Student number: ${preferences.studentNumber}` : 'Add your student number in Settings.'}</p><Link className="text-link" style={{ fontSize: 12 }} href="/settings">Edit assessment details</Link></div></div></section><section className="content-panel"><h2>Two ways to practise</h2><ul><li><strong>Wordle:</strong> choose phoneme symbols to build a three-, four- or five-sound guess. Colours, symbols and text show which sounds belong in the word. The completed word appears alongside its English equivalent.</li><li><strong>Word Search:</strong> find five or ten phoneme sequences in a generated grid. Drag along a word, select its endpoints, or use arrow keys and Enter. Show answers provides a practice guide.</li><li><strong>Difficulty:</strong> Wordle offers 8, 6 or 4 guesses. Word Search suggests 7 × 7, 10 × 10 or 12 × 12 grids, supports custom rows and columns, and adds directions at higher levels.</li></ul></section></div><section className="content-panel"><div className="card-title"><span className="feature-icon"><Video size={20}/></span><h2>Your video walkthrough</h2></div><p>Add your recorded demonstration and verbal design justification here. The guide below covers what to show and explain.</p>{videoSource ? <><video key={videoSource} className="walkthrough-video" controls preload="metadata" src={videoSource} onError={() => setVideoError(true)} aria-label="Website demonstration and design justification">Your browser does not support this video.</video>{videoError && <p className="error-text">This video could not be loaded. Use a direct MP4 or WebM link, or select a file below.</p>}</> : <div className="video-placeholder"><Video size={32}/><strong>A place for your walkthrough</strong><p>Record a short demonstration, then choose your video to preview it here.</p></div>}<div style={{ marginTop: 17, marginBottom: 18 }}><label className="file-label"><Upload size={15}/>Choose a video<input className="sr-only" type="file" accept="video/mp4,video/webm,video/quicktime" onChange={e => { const file = e.target.files?.[0]; if (!file)
        return; if (!file.type.startsWith('video/')) {
        toast.error('Please choose a video file.');
        return;
    } setLocalVideo(URL.createObjectURL(file)); setVideoError(false); }}/></label><p className="field-help" style={{ marginTop: 8 }}>Local files are previewed on this page only and are not uploaded. A saved video link stays on this browser.</p></div><Field id="video-url" label="Or use a direct video link"><input id="video-url" className="text-input" type="url" maxLength={500} placeholder="https://example.com/walkthrough.mp4" value={urlDraft} onChange={e => setUrlDraft(e.target.value)} onBlur={setVideoUrl} onKeyDown={e => { if (e.key === 'Enter')
        setVideoUrl(); }}/></Field><details className="recording-script" open><summary>Recording guide & verbal justification</summary><p><strong>1. Introduce the project.</strong> “I built Phoneme Studio to help teachers create activities for speech pathology students. Assessment 1 is frontend only, so the word corpus and example sets are bundled with the app and no database is required.”</p><p><strong>2. Show the workflow.</strong> Open Wordle and choose train from the HCE corpus. Change its title and difficulty, and explain the four tiles t, ɹ, æɪ, n. Hover over /θ/ to show TH as in thin. Enter an incorrect guess, then t, ɹ, æɪ, n to reveal TRAIN. Switch to a three- or five-phoneme answer to demonstrate how the grid adapts. Generate the HTML and open it in a browser. Repeat with Word Search and find a word using its first and last tiles.</p><p><strong>3. Explain the design.</strong> “The settings and preview sit side by side so teachers can see the effect of their choices. On smaller screens they stack vertically. A consistent navigation bar, clear labels and a short workflow keep the builders easy to learn.”</p><p><strong>4. Explain component structure.</strong> “Shared components handle navigation, form fields, support switches and the preview. Phoneme data, puzzle generation and HTML export are kept separate. The preview and downloaded file use the same game runtime, which keeps their behaviour consistent. Later, the fixed word data can be replaced by a database-backed source.”</p><p><strong>5. Cover accessibility.</strong> “The app supports keyboard navigation, visible focus, labelled controls and light or dark themes. Wordle feedback uses symbols and text as well as colour. The word search supports arrow keys and Enter. Hover hints also work on focus, and there is no timer, so students can work at their own pace.”</p><p><strong>6. Discuss trade-offs.</strong> “The games use the supplied broad HCE transcriptions and fixed example sets. Guesses are sound sequences rather than dictionary-validated words. The standalone HTML uses a small JavaScript runtime so it needs no internet connection or extra files. Teacher preferences are stored in cookies, but there are no student records or progress tracking. The answer is contained in the downloaded file, so this is a learning activity rather than a secure test.”</p></details></section></div></Shell>;
}
